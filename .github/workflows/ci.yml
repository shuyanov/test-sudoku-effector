name: Run Selenium Tests and Generate Report

on:
  push:
    branches: [master]

jobs:
  run-selenium:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Run Selenium tests and generate report
        run: npx mocha test/timer.test.js
